# IMAP (수신)
IMAP_HOST=imap.zoho.com
IMAP_PORT=993
IMAP_USER=
IMAP_PASSWORD=
IMAP_SECURE=true

# SMTP (발신)
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=true

# 브릿지 동작
POLL_INTERVAL_SEC=120
SUBJECT_PREFIX=[CAIA-JOB]
REPLY_FROM= # 보낸사람 표시(없으면 SMTP_USER 사용)

# 젠스파크 전달 모드(이메일 브릿지)
ZENSPARK_INBOX=jobs@caia-agent.com   # 필요 시 변경
