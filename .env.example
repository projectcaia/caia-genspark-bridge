# 인바운드 보안(Cloudflare Worker JSON 사용 시)
CAIA_INBOUND_SECRET=<랜덤_긴_문자열>   # 예: openssl rand -hex 32

# 발신: SendGrid Web API (SMTP 대신 Web API로 안정 전송)
SENDGRID_API_KEY=<SG_xxx>

# 브릿지 공통
SUBJECT_PREFIX=[CAIA-JOB]
REPLY_FROM=axel.nam@caia-agent.com
ZENSPARK_INBOX=axel.nam@caia-agent.com   # 젠스파크 수신 메일 주소

# (선택) 진단 전용 수신자
DIAG_TO=axel.nam@caia-agent.com
